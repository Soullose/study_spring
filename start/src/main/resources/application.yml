server:
  port: 8080
  error:
    include-exception: true
    include-message: always

spring:
  output:
    ansi:
      enabled: detect
  profiles:
    include: jpa,datasource,redis,swagger2
    #打包的程序输出的异常信息前端可以显示
