server:
  port: 40001
  error:
    include-exception: true
    include-message: always

spring:
  output:
    ansi:
      enabled: detect
  profiles:
    #,redis,swagger2
    include: jpa,datasource
    #打包的程序输出的异常信息前端可以显示

