spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    open:
      # url: jdbc:postgresql://127.0.0.1:5432/postgres
      # username: postgres
      # password: 123456789
      # driver-class-name: org.postgresql.Driver
      url: jdbc:mysql://localhost:3306/open?serverTimezone=UTC&characterEncoding=utf8&useUnicode=true&useSSL=false&allowPublicKeyRetrieval=true
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
